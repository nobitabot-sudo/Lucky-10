const express = require('express');
const router = express.Router();

// This file can be used for additional auth routes if needed
// Currently, auth routes are in users.js

module.exports = router;
